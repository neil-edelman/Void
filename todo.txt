fog
fill in space between planets and foreground, etc
build up asteroids from smaller chunks; hit them and calculate new chunks
instead of having the same code for collision detection and display, have a nearest sector for display instead of is_selected; should be O(Sqrt[n]) -> O(n)
